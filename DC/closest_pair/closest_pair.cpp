#include <iostream>
using namespace std;

// If consider every pair = 2T(n/2) + O(n^2) => O(n^2)
// If use Divide and Conquer from X-axis = 2(T/2) + O(6n/2) => O(nlogn)

// A structure to represent a Point in 2D plane
class Point
{
public:
    int x;
    int y;
    Point(int xIn, int yIn) {
        x = xIn;
        y = yIn;
    }
}

main(int argc, char const *argv[])
{
    Point p1(0, 0), p2()
    Point pt_xsort[] = {};
    Point pt_ysort[] = {};
    closest_pair(arr);
    for (auto &x : arr)
        cout << x << " ";
    return 0;
}